[Unit]
Description=GPSD standalone (no socket activation)
After=network.target
Wants=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/gpsd -N -n -F /run/gpsd.sock /dev/ttyACM0
Restart=on-failure
RestartSec=2

[Install]
WantedBy=multi-user.target
