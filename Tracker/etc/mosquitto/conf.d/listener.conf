# Listen on all IPv4 interfaces on port 1883 (LAN + localhost)
listener 1883 0.0.0.0

# For a private LAN/VLAN this is fine.
# If you ever expose this beyond your LAN, we should add auth.
allow_anonymous true
